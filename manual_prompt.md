# 多店舗在庫管理システム マニュアル作成プロンプト

以下のプロンプトをChatGPT・Claude・Gemini等のAIにそのまま貼り付けてください。

---

## ▼ ここからプロンプト ▼

あなたはドキュメント作成の専門家です。
以下に示すアプリの仕様情報をもとに、**エンドユーザー向けの操作マニュアル（PDF用）**を日本語で作成してください。

### 出力形式の指示

- PDF印刷を前提としたHTML形式で出力してください
- A4縦を想定したレイアウト（最大幅 900px 程度）
- 日本語フォント（Hiragino / Meiryo）
- 章ごとに番号付き見出しを使用
- 表・番号付き手順・注意ボックスを適切に使用
- 表紙・目次・各章・FAQ・フッターを含めること
- 印刷時に余白（margin: 20mm 程度）が確保されるようにすること
- `@media print` でナビゲーション要素を非表示にすること

---

### スプレッドシート駆動の仕組み（重要）

このマニュアルの実体は `web/help/manual.html` に配置されています（`flutter build web` 時に `web/` 配下がそのまま `build/web/` にコピーされ、Firebase Hostingで `https://multi-store-inventory-app.web.app/help/manual.html` として配信される）。アプリ内からは設定画面の「マニュアル・使い方」から新しいタブで開けます。

このマニュアルは、TANOMUプロジェクトの `web/help/assets/help.js` の `loadSheetManual()` と同じ方式で、Googleスプレッドシートの内容を実行時に取得して本文を差し替えられる構造になっています。マニュアルを再生成・編集する際は、この仕組みを壊さないよう以下を守ってください。

- `<body data-help-sheet-id="..." data-help-sheet-gid="...">` にスプレッドシートID・gidをdata属性としてハードコードする（ビルド・デプロイ不要、都度fetch方式）
- 各章は `<div class="section" id="sN" data-section-id="スラッグ">` とし、見出しは `<span class="section-title-text">` で囲む。本文は `<div class="section-body">` で囲む
- ページ読み込み時に `loadSheetManual()` が下記CSVエクスポートURLを無認証でfetchする
  `https://docs.google.com/spreadsheets/d/{sheetId}/gviz/tq?tqx=out:csv&gid={gid}&cachebust={Date.now()}`
- CSVの列構成（1行目はヘッダーとして読み飛ばす）:

  | 列 | 内容 |
  |----|------|
  | section_id | 章を識別するスラッグ（例: pos, store_list, item_master） |
  | section_title | 章タイトル（h2の`.section-title-text`に反映） |
  | content_html | 章本文（HTMLタグを含めてよい。`.section-body`にinnerHTMLとして反映） |
  | order | 表示順（昇順ソートし、目次・章番号もこの順で再構成される） |

- 取得・解析に成功しCSVに有効な行がある場合のみ本文を差し替える。取得失敗・シート未公開・解析エラー時は何もせず、HTML内の既存静的本文をそのまま表示する（フォールバック）
- 章スラッグ一覧: overview, setup, login, store_list, store_inventory, all_store_inventory, item_master, order_list, special_order, pos, history, store_reorder, org_management, ad_management, account_settings, feedback, permissions, faq

---

### アプリ基本情報

| 項目 | 内容 |
|------|------|
| アプリ名 | 多店舗在庫管理システム |
| バージョン | 1.3.0 |
| プラットフォーム | Webブラウザ（PC・スマートフォン）/ Android APK |
| Web URL | https://multi-store-inventory-app.web.app |
| 開発会社 | Happy Bluebird Co., Ltd. |
| お問い合わせ先 | info@happy-bluebird.co.jp |
| 対象ユーザー | 複数店舗を持つ小売・美容サロン等の店舗スタッフ |

---

### ユーザー権限

| 権限 | 説明 |
|------|------|
| 管理者（admin） | 組織作成者。組織管理・広告管理・店舗追加が可能 |
| メンバー（member） | 招待コードで参加したスタッフ。在庫操作・発注が可能 |

---

### 全機能一覧と詳細仕様

#### 【1】アカウント・認証

**新規登録**
- メールアドレス＋パスワード（6文字以上）で登録
- 登録後に組織設定画面へ遷移

**ログイン**
- メール＋パスワードでログイン
- パスワードリセット機能あり（メール送信）

**ログアウト**
- メニュー（右上 ⋮）→「ログアウト」

**パスワード変更**
- メニュー→「パスワード変更」
- 現在のパスワードで再認証後、新パスワード（6文字以上）を設定

**ニックネーム変更**
- メニュー→「ニックネーム変更」
- ニックネームは履歴の記録者として使用される

**アカウント削除**
- メニュー→「アカウント削除」
- パスワード入力で本人確認後、完全削除（取り消し不可）

---

#### 【2】初回セットアップ（組織設定）

ログイン後に組織が未設定の場合、組織設定画面が表示される。

**A. 新しい組織を作成（管理者になる）**
1. 「新しい組織を作成」を選択
2. 組織名・組織コード（英小文字・数字・アンダースコアのみ）・ニックネームを入力
3. 利用規約・プライバシーポリシーに同意
4. 「作成」→ 承認待ち画面へ遷移（統括管理者による承認が必要）
5. 承認後に利用開始

**B. 既存の組織に参加（メンバーになる）**
1. 「既存の組織に参加」を選択
2. 管理者から共有された招待コードを入力
3. ニックネームを入力
4. 利用規約・プライバシーポリシーに同意
5. 「参加」→ 即時利用開始

**制限**
- ユーザー数の上限（デフォルト5人）に達している場合は参加不可
- 店舗数の上限（デフォルト5店舗）に達している場合は店舗追加不可

---

#### 【3】店舗一覧（メイン画面）

ログイン後の最初の画面。

**表示内容**
- 上部: 組織名
- 「店舗数: ○ 件」カード
- 各店舗カード（タップで店舗別在庫ページへ）
- 下部: 広告バナー（設定時のみ・1分ごとにローテーション）

**管理者による店舗追加**
- 右下の「＋」ボタン→ 店舗名・コード・店舗ID（英数字）を入力して追加
- 上限（デフォルト5店舗）に達するとエラー表示

**メニュー一覧（右上 ⋮）**
- 全店舗在庫確認（全員）
- 修正・追加履歴（全員）
- 商品マスタ管理（全員）
- 発注リスト（全員）
- 特別発注・新規発注（全員）
- レジ（全員）
- 店舗の並び替え（全員）
- 組織管理（管理者のみ）
- 広告スペース管理（管理者のみ）
- ニックネーム変更（全員）
- パスワード変更（全員）
- 組織を脱退（メンバーのみ）
- 利用規約（全員）
- プライバシーポリシー（全員）
- 機能追加依頼 / 修正依頼 / バグ報告（全員）
- ログアウト（全員）
- アカウント削除（全員）
- バージョン表示（v1.3.0）

---

#### 【4】店舗別在庫管理

店舗カードをタップして開く。

**タブ**
- 商品 / テスター / 備品 の3タブ

**在庫数の表示ルール**
- 通常: 黒字で表示
- 基準在庫を下回る場合: 赤字で表示
- 発注済みがある場合: 「発注済: ○個」バッジを表示

**在庫数の変更操作**
| 操作 | 方法 |
|------|------|
| 1増やす | ＋ボタンをタップ |
| 1減らす | −ボタンをタップ（0以下にはならない） |
| 直接指定 | 在庫数をタップ → 数値入力ダイアログ |

**保存**
- ＋/−/直接入力で変更が生じると画面下部に「保存」ボタンが出現
- 「保存」タップ → 確認ダイアログ → Firestoreへ保存
- 変更件数・内容が確認ダイアログに表示される
- 保存と同時に履歴にニックネームつきで記録される

**基準在庫の設定**
- アイテム行を長押し → 「基準在庫: ○○」ダイアログ → 数値入力 → 保存
- 基準在庫は0以上の整数

**納品処理**
- 「発注済: ○個」バッジのあるアイテムに「納品」ボタンが表示される
- 「納品する」をタップ → 確認ダイアログ → 発注数が在庫数に加算 → 発注済みがクリアされる

**検索**
- 上部検索バーでコードまたは名前を絞り込み

**再読み込み**
- 右上の更新アイコンで最新データを取得

---

#### 【5】全店舗在庫確認

メニュー→「全店舗在庫確認」

- 商品 / テスター / 備品 の3タブ
- 縦軸: アイテム名、横軸: 店舗名のテーブル形式
- 基準在庫を下回るセルは赤字
- 読み取り専用（変更不可）

---

#### 【6】商品マスタ管理

メニュー→「商品マスタ管理」

**タブ**
- 商品 / テスター / 備品

**アイテムの追加**
1. 右下「＋」ボタン
2. コード（任意）・名前（必須）を入力
3. 「保存」

**レジ用価格・軽減税率（商品タブのみ）**
- 「商品」タブの追加・編集ダイアログでは、コード・名前に加えて税抜価格・軽減税率（8%）を設定可能
- 入力した税抜価格から税込価格（10%または8%）を自動プレビュー表示
- ここで設定した税抜価格・税率が「レジ」機能の会計金額計算に使用される
- 税抜価格が未登録（0円）の商品はレジで会計不可

**アイテムの編集**
- 行をタップ → 編集ダイアログ → コード・名前（商品タブは税抜価格・軽減税率も）を変更 → 「保存」

**アイテムの削除**
- 行を長押し（またはスワイプ）→ 削除確認 → 「削除」

**販売終了（廃番）設定**
- 編集ダイアログで「販売終了」をON
- 販売終了アイテムは発注リストから自動除外、在庫一覧でグレー表示

**並び順**
- コードのナチュラルソート順（例: T1 < T2 < T10）
- コードなしの場合は名前順

**検索**
- 上部検索バーで絞り込み

---

#### 【7】発注リスト

メニュー→「発注リスト」（※広告表示後に開く場合あり）

**表示条件**
- 基準在庫が設定されていて、かつ現在在庫が基準在庫を下回っているアイテムのみ表示
- 販売終了アイテムは除外

**表示内容**
- 店舗名・アイテム名・カテゴリ
- 現在庫 / 基準在庫 / 不足数
- 発注数量（変更可能、デフォルトは不足数）
- 発注済みバッジ（発注済みの場合）

**絞り込み**
- 上部チェックボックスで「商品 / テスター / 備品」を選択

**発注操作**
1. 発注数量を確認・変更
2. 「発注」ボタン
3. 確認ダイアログ「発注する」

**PDF出力**
- 右上の印刷アイコン → A4サイズのPDF生成 → 印刷または保存

---

#### 【8】特別発注・新規発注

メニュー→「特別発注・新規発注」

**対象**
通常の発注リスト（基準在庫管理）とは別に管理する特別品・新規品。

**発注種別**
- 特別発注 / 新規発注 / その他

**アイテム追加**
1. 右下「＋」ボタン
2. アイテム名・種別・メモ（任意）を入力
3. 「追加」

**仮発注**
- 各アイテム行の店舗欄に数量を入力 → 「仮発注する」確認ダイアログ → 確定

**納品処理**
- 「納品する」ボタン → 確認 → 納品済みとしてマーク

**アイテム削除**
- 削除ボタンでアイテムを削除

---

#### 【9】レジ・会計

メニュー→「レジ」

**概要**
店舗店頭での会計処理を行い、レシート（領収書）PDFを発行する。商品コード指定の通常会計と、金額を都度入力する手入力会計の2種類がある。

**販売店舗・インボイス番号**
- 「販売店舗」ドロップダウンで会計する店舗を選択
- 「インボイス事業者番号」（Tから始まる登録番号）を入力・保存 → レシートに印字される

**商品会計（商品コード指定）**
1. 「商品コード」欄にコードを入力 → 「検索」（またはEnter）
2. 該当商品の税抜単価・税込単価（軽減税率対応）が表示される
3. 「数量」「預かり金」を入力 → 合計・おつりが自動計算
4. 「会計確定・レシートPDF印刷」→ 確認ダイアログ「確定する」

**在庫連動**
- 商品会計を確定すると、選択店舗の在庫が数量分自動的に減算される
- 在庫が数量に満たない場合はエラーとなり会計不可

**金額手入力（商品マスタにない会計）**
1. 「金額手入力」ボタンをタップ
2. 「内容・品名」「税込単価」「数量」を入力
3. 「税率」で10%または8%（軽減税率）を選択
4. 「預かり金」を入力 → 「手入力会計確定・レシートPDF印刷」

**手入力会計の注意点**
- 在庫数は変更されない（レシートに「在庫変更なし」と記載）

**会計確定後の動作**
- 領収書PDFが自動生成され、印刷・共有（Printing）が可能
- PDFは保存され、取引履歴から再印刷できる
- 領収書には発行日・店舗名・インボイス登録番号・品名・数量・税抜/税込単価・消費税額・預かり金・おつり・在庫更新内容を記載

**取引履歴**
- レジ画面右上の履歴アイコンから直近100件の取引を新しい順に一覧表示
- 表示内容: 店舗名・日時・区分（商品/金額手入力）・預かり金・おつり・合計金額
- 各取引の「印刷」ボタンで保存済みレシートPDFを再印刷可能

---

#### 【10】修正・追加履歴

メニュー→「修正・追加履歴」

- 直近100件を新しい順で表示
- 右上更新アイコンで最新取得

**各履歴の表示内容**
| 項目 | 内容 |
|------|------|
| 日時 | 変更された日時 |
| 店舗名 | 変更が行われた店舗 |
| アイテム名 | 変更されたアイテム |
| 種別 | 商品 / テスター / 備品 |
| 変更前 → 変更後 | 変更前後の在庫数 |
| 記録者 | 変更したユーザーのニックネーム |

---

#### 【11】店舗の並び替え

メニュー→「店舗の並び替え」

1. 各行右端の「≡」アイコンをドラッグして順番を変更
2. 「保存」で確定

---

#### 【12】組織管理（管理者専用）

メニュー→「組織管理」

**組織名の変更**
- 「組織名」欄を編集して保存

**ロゴのアップロード**
1. 「ロゴを変更」
2. 画像ファイル選択（JPEG/PNG推奨、幅300px・quality55でリサイズ保存）
3. トップバーにロゴが表示される

**招待コードの管理**
- 現在の招待コードを確認・コピー
- 「招待コードを変更」で新しいコードを設定（英数字・アンダースコアのみ）
- 変更すると古いコードは無効になる

**メンバー一覧**
- 管理者→メンバーの順でソート表示
- 表示内容: ニックネーム・メールアドレス・役職
- メンバー削除: 行右端のアイコン → 確認 → 削除（取り消し不可）

**組織の削除**
- 「組織を削除」で組織全体を削除（取り消し不可）

**組織を脱退（メンバー専用）**
- メニュー→「組織を脱退」→ 確認 → 脱退
- 脱退後は新しい組織作成または別の組織に参加可能

---

#### 【13】広告スペース管理（管理者専用）

メニュー→「広告スペース管理」

**広告モード**
| モード | 説明 |
|--------|------|
| 非表示 | 広告を一切表示しない |
| 自社メッセージ | 自組織の広告のみ表示 |
| 配信広告受信 | 他組織の広告も含めて表示 |

**広告スロット設定**
- 最大3スロット（通常）まで登録可能
- 各スロットに画像・メッセージ・URL（タップ時の遷移先）を設定

**広告の表示タイミング**
- 店舗一覧下部のバナー: 1分ごとにローテーション
- 発注リストを開く直前: 3秒間の全画面広告（3秒後に自動クローズ）

---

#### 【14】フィードバック送信

メニューから送信可能。

| 種別 | メニュー項目 | 用途 |
|------|-------------|------|
| 機能追加依頼 | 「機能追加依頼」 | 新機能リクエスト（無料、対応保証なし） |
| 修正依頼 | 「修正依頼」 | 既存機能の改善リクエスト |
| バグ報告 | 「バグ報告」 | 不具合・エラー報告 |

送信先: info@happy-bluebird.co.jp（メールアプリが起動します）

---

### 権限対応表

| 機能 | 管理者 | メンバー |
|------|--------|----------|
| 在庫確認・変更 | ○ | ○ |
| 基準在庫設定 | ○ | ○ |
| 商品マスタ管理 | ○ | ○ |
| 発注リスト・発注操作 | ○ | ○ |
| 特別発注管理 | ○ | ○ |
| レジ・会計 | ○ | ○ |
| 履歴確認 | ○ | ○ |
| 店舗の並び替え | ○ | ○ |
| ニックネーム変更 | ○ | ○ |
| パスワード変更 | ○ | ○ |
| フィードバック送信 | ○ | ○ |
| 店舗追加 | ○ | × |
| 組織管理 | ○ | × |
| メンバー管理 | ○ | × |
| 広告スペース管理 | ○ | × |
| 組織を脱退 | × | ○ |

---

### よくある質問（FAQとして必ず含めること）

**Q. 在庫を変更したのに保存されていない**
A. ＋/−ボタン等で変更後、必ず画面下部の「保存」ボタンをタップしてください。タップせずに画面を離れると変更は破棄されます。

**Q. 発注リストにアイテムが表示されない**
A. 「基準在庫が設定されていて、かつ現在在庫が基準在庫を下回っている」アイテムのみ表示されます。また「販売終了」設定のアイテムは除外されます。

**Q. 新しい組織を作ったがログイン後に使えない**
A. 新規組織は統括管理者の承認が必要です。承認されるまで「承認待ち」画面が表示されます。承認後はアプリを再起動してください。

**Q. 招待コードがわからない**
A. 管理者が「組織管理」→「招待コード」から確認・共有できます。メンバーは管理者にお問い合わせください。

**Q. パスワードを忘れた**
A. ログイン画面「パスワードをお忘れですか？」からメールアドレスを入力するとリセットメールが届きます。

**Q. 履歴に記録者名が表示されない**
A. ニックネーム機能追加前の変更は記録者名が空白になる場合があります。以降の変更は正しく記録されます。

**Q. 広告を非表示にしたい**
A. 管理者が「広告スペース管理」→「非表示」モードを選択することで広告を無効化できます。

**Q. 店舗数・ユーザー数の上限を増やしたい**
A. 上限変更は統括管理者のみが行えます。「フィードバック→機能追加依頼」またはinfo@happy-bluebird.co.jp までご連絡ください。

**Q. レジで商品コードを検索しても会計できない**
A. 「商品マスタに税抜価格が未登録です」と表示される場合、商品マスタ管理でその商品の税抜価格を設定してください。価格未登録（0円）の商品はレジで会計できません。

**Q. レジで在庫が足りないと言われる**
A. 商品会計は在庫を自動で減算するため、在庫数が数量に満たない場合は会計できません。在庫を補充するか、在庫を変更しない「金額手入力」を利用してください。

---

### マニュアルの構成指示

以下の順番で章を構成してください:

1. 表紙（アプリ名・バージョン・Web URL）
2. 目次（各章へのアンカーリンク付き）
3. アプリ概要
4. はじめに（初回セットアップ）
5. ログイン・ログアウト
6. 店舗一覧（メイン画面）とメニュー
7. 店舗別在庫管理
8. 全店舗在庫確認
9. 商品マスタ管理
10. 発注リスト
11. 特別発注・新規発注
12. レジ・会計
13. 修正・追加履歴
14. 店舗の並び替え
15. 組織管理（管理者専用）
16. 広告スペース管理（管理者専用）
17. アカウント設定
18. フィードバック送信
19. 権限一覧（表形式）
20. よくある質問（FAQ）

---

それでは、上記の仕様に基づき、PDF印刷に適したHTMLマニュアルを作成してください。
